# دليل البدء السريع - منصة أمانة

## 1. التشغيل السريع ⚡

### تطبيق العملاء (Customer App)
1. افتح Android Studio
2. اختر `customer-app` من قائمة Run Configurations
3. شغّل التطبيق
4. سجل حساب جديد كعميل

### تطبيق مقدمي الخدمات (Provider App)
1. افتح Android Studio
2. اختر `provider-app` من قائمة Run Configurations
3. شغّل التطبيق  
4. سجل حساب جديد كمزود خدمة

## 2. سيناريو الاستخدام الكامل 📝

### أ. تسجيل مقدم خدمة:
1. افتح `provider-app`
2. اضغط "ليس لديك حساب؟ سجل الآن"
3. أدخل:
   - رقم الهاتف: `0501234567`
   - الاسم: `محمد السباك`
   - المدينة: `الرياض`
   - الحي: `الملقا`
   - النبذة: `خبرة 10 سنوات في السباكة`
4. اضغط "تسجيل كمزود خدمة"

### ب. شحن المحفظة:
1. اذهب إلى تبويب "المحفظة"
2. اضغط "شحن المحفظة"
3. أدخل كود الشحن (يجب على المشرف توليده)
4. سيتم إضافة الرصيد تلقائياً

### ج. تسجيل عميل وطلب خدمة:
1. افتح `customer-app`
2. سجل حساب جديد
3. اختر "سباكة" من التصنيفات
4. اختر مزود خدمة
5. أرسل طلب

### د. إرسال عرض سعر:
1. في `provider-app`، اذهب لتبويب "الطلبات"
2. ستجد الطلب الجديد
3. اضغط "إرسال عرض سعر"
4. أدخل السعر: `200` ريال
5. **سيتم التحقق من الرصيد** (200 × 0.15 = 30 ريال عمولة)
6. إذا كان الرصيد كافٍ، سيتم إرسال العرض

### هـ. قبول العرض وإتمام الطلب:
1. في `customer-app`، اذهب لـ "طلباتي"
2. افتح الطلب واضغط "قبول"
3. في `provider-app`، اضغط "بدء العمل"
4. بعد الانتهاء، اضغط "إتمام الطلب"
5. **سيتم خصم العمولة تلقائياً** من المحفظة

## 3. توليد كود شحن (للمشرف) 🎫

يمكن للمشرف توليد كود شحن عبر:

```kotlin
// في Admin App أو من خلال كود مباشر
val repository = AmanaRepository(context)
val code = repository.generateRechargeCode(amount = 100.0)
println("كود الشحن: ${code.code}")
// مثال: AMANA-A1B2C3D4
```

## 4. اختبار نظام العمولات 💡

### سيناريو 1: رصيد كافٍ ✅
```
رصيد المزود: 100 ريال
عرض السعر: 200 ريال
العمولة المطلوبة: 200 × 0.15 = 30 ريال
النتيجة: ✅ يتم إرسال العرض
```

### سيناريو 2: رصيد غير كافٍ ❌
```
رصيد المزود: 20 ريال
عرض السعر: 200 ريال
العمولة المطلوبة: 30 ريال
النتيجة: ❌ رفض + رسالة "رصيد المحفظة غير كافٍ"
```

### سيناريو 3: إتمام الطلب 💸
```
رصيد قبل الإتمام: 100 ريال
العمولة: 30 ريال
رصيد بعد الإتمام: 70 ريال
✅ تسجيل في جدول Transactions
```

## 5. قاعدة البيانات 🗄

الموقع على الجهاز:
```
/data/data/com.amana.customer/databases/amana.db
/data/data/com.amana.provider/databases/amana.db
```

عرض قاعدة البيانات:
```bash
adb shell
cd /data/data/com.amana.customer/databases
sqlite3 amana.db
.tables
SELECT * FROM users;
```

## 6. استكشاف الأخطاء 🔧

### مشكلة: التطبيق لا يعمل
```
الحل:
1. File → Invalidate Caches
2. Build → Clean Project
3. Build → Rebuild Project
```

### مشكلة: خطأ في Gradle
```
الحل:
./gradlew clean
./gradlew build
```

### مشكلة: قاعدة البيانات فارغة
```
الحل:
1. حذف التطبيق من الجهاز
2. إعادة التثبيت
3. ستُنشأ قاعدة بيانات جديدة تلقائياً
```

## 7. نصائح للتطوير 💻

### إضافة تصنيف خدمة جديد:
```kotlin
// في AmanaDbHelper.kt → seedReferenceData()
Triple("11", "تمديد كهرباء", "💡"),
```

### تعديل نسبة العمولة:
```kotlin
// في AmanaRepository.kt
companion object {
    const val COMMISSION_RATE = 0.20 // 20% بدلاً من 15%
}
```

### إضافة حقل جديد:
1. أضف في `Models.kt`
2. أضف في `AmanaDbHelper.kt` (CREATE TABLE)
3. أضف في `AmanaDao.kt` (cursor mapping)
4. زِد `DB_VERSION`

## 8. البيانات التجريبية 🧪

### مستخدم عميل تجريبي:
```
الهاتف: 0500000001
الاسم: أحمد العميل
المدينة: الرياض
الحي: العليا
```

### مستخدم مزود تجريبي:
```
الهاتف: 0500000002
الاسم: محمد المزود
المدينة: الرياض
الحي: الملقا
الخبرة: 10 سنوات
```

## 9. الأكواد المهمة 🔑

### كود شحن تجريبي (يجب توليده من Admin):
```
يمكن توليد أكواد عبر Admin Panel
أو عبر كود مباشر كما في القسم 3
```

## 10. الدعم 📞

للمساعدة:
- راجع `README.md` الرئيسي
- اقرأ التعليقات في الكود
- تحقق من Logcat في Android Studio

---

**ملاحظة:** هذا المشروع جاهز للاستخدام والتطوير الفوري! 🎉
