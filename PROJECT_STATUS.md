# حالة مشروع أمانة

آخر تحديث: 12 يوليو 2026

هذه الوثيقة هي المرجع الصريح لحالة التنفيذ. وجود شاشة أو جدول محلي لا يعني
أن الميزة مكتملة أو صالحة للإنتاج.

## منفذ فعليًا

- مشروع Android قديم متعدد الوحدات يحتوي واجهات Kotlin/Jetpack Compose
  للعميل ومقدم الخدمة، ومصادر واجهات Android للإدارة. هذا وصف لوجود المصدر
  وليس تأكيدًا بأن كل الوحدات تُبنى.
- طبقة SQLite محلية وتواصل بين تطبيقات Android عبر `ContentProvider`.
- معرض `web-app/` ثابت يضم 49 ملف HTML من Stitch للرجوع البصري فقط.
- أساس Flutter جديد في `apps/amana_flutter/`:
  - RTL وعربية.
  - Material 3 وثيم مشترك.
  - بوابة ومسارات منفصلة للعميل ومقدم الخدمة والإدارة.
  - تنقل متجاوب للهاتف والشاشات الواسعة.
  - اختيار target وقت البناء عبر `AMANA_ROLE`.
  - دورة محلية قابلة للتجربة: نشر طلب، إرسال عرض، قبول العرض، وعرض
    أثر العمولة في محفظة مقدم الخدمة.
- أساس Backend مركزي في `backend/supabase/`:
  - PostgreSQL migration أولية.
  - Supabase Auth/JWT كأساس للمصادقة.
  - Row Level Security أولية.
  - Realtime للرسائل وتغييرات الطلبات.
  - RPCs ذرية لإرسال العرض وقبوله، مع التحقق من رصيد المزود وخصم عمولة 15%.

## Scaffold فقط

- واجهات Flutter الحالية تثبت المعمارية والتنقل وليست تنفيذًا للشاشات الـ49.
- دورة الطلب الحالية في Flutter تستخدم `DemoOrderFlow` داخل الذاكرة لإثبات
  الرحلة فقط؛ لم تُربط بعد بـ RPCs وقاعدة البيانات.
- اختيار الدور داخل واجهة Flutter تجريبي. في الإنتاج يجب أن يأتي الدور من
  جلسة Auth/JWT موثقة.
- مخطط Supabase أساس أولي ويحتاج مراجعة migrations واختبارات سياسات RLS قبل
  أي نشر.
- فصل targets الثلاثة يتم حاليًا من codebase واحد باستخدام `--dart-define`؛
  لم تُنشأ بعد حزم Android/Web/Windows الناتجة لأن توفر Flutter SDK لم يمكن
  التحقق منه في هذه الجلسة.

## Mock أو محلي فقط

- بيانات وتسجيل الدخول في تطبيقات Android الحالية محلية، وليست حسابات مركزية.
- SQLite الحالية لا تزامن البيانات بين أجهزة العملاء والمزودين والإدارة.
- `web-app/` معرض HTML static ولا يمثل تطبيق ويب وظيفيًا.
- بيانات لوحات Flutter الحالية نصوص حالة فقط ولا تتصل بقاعدة البيانات.

## مشكلات مؤكدة في Android القديم

- `settings.gradle.kts` لا يضم وحدة `:app` التي يفترضها الكود كمالك قاعدة
  SQLite/ContentProvider، لذلك تصميم مشاركة البيانات المحلي غير مكتمل ضمن
  إعداد Gradle الحالي.
- `admin-app` يحتوي مصادر Fragment/RecyclerView/ViewBinding قديمة، بينما ملف
  Gradle الحالي لا يعلن تبعياتها ولا يفعّل ViewBinding. لا يصح وصف الوحدة
  بأنها تطبيق إدارة مكتمل أو مضمون البناء.
- يوجد أكثر من مسار/جيل للكود داخل `core` و`admin-app`؛ يجب استخلاص السلوك
  المطلوب منه بدل البناء عليه كمعمارية إنتاج.

## غير منفذ بعد

- ربط Flutter بـ Supabase SDK وتدفق OTP/الهاتف.
- حفظ JWT آمن، refresh، واسترجاع الدور من الخادم.
- onboarding موثوق لمقدم الخدمة، ومنح أدوار الإدارة من جهة الخادم فقط.
- ربط واجهة Flutter بوظائف الطلب والعرض والعمولة الموجودة في Backend.
- رفع الصور، الخرائط، push notifications، المدفوعات، والمراقبة.
- تحويل تصاميم Stitch إلى widgets فعلية واختبارات قبول.
- CI وبناء APK/AAB/Web/Windows موقّع.
- اختبارات تكامل للـ Backend وRLS وRealtime.

## ملاحظات أمان

- يحتوي `local.properties` المحلي على قيمة Google Maps API فعلية ظاهريًا.
  الملف مستثنى من Git، لكن يجب تقييد المفتاح من Google Cloud Console وتدويره
  إذا سبق مشاركته.
- لا توجد أسرار حقيقية في scaffold الجديد. `backend/.env.example` placeholders
  فقط.
- كلمة مرور الإدارة الثابتة في Android القديم ليست مصادقة إنتاج ويجب إلغاؤها
  عند الانتقال إلى Backend.

## عوائق التحقق الحالية

- Flutter موجود في `C:\flutter\flutter_windows_3.44.2-stable\flutter`، لكن أمر
  فحص النسخة ظل عالقًا في بيئة الطرفية، لذلك لم يمكن توليد platform shells
  أو تشغيل `flutter test` بعد.
- لا يوجد مجلد `.git` ظاهر داخل المشروع، لذلك لا يمكن تقديم حالة Git موثوقة.
- لا يوجد APK داخل `build/outputs/apk` وقت الفحص.

راجع `ARCHITECTURE.md` للبنية المستهدفة وخطوات المتابعة.
